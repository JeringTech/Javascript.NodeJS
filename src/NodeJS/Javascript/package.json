{
    "private": true,
    "scripts": {
        "build": "echo ------ Yarn Install ------ && yarn install && echo ------ Webpack Compile ------ && webpack"
    },
    "dependencies": {
        "@types/node": "^18.11.18",
        "ts-loader": "^9.2.6",
        "typescript": "^4.9.4",
        "webpack": "^5.75.0",
        "webpack-cli": "^5.0.1"
    }
}
