{
    "private": true,
    "scripts": {
        "build": "echo ------ Yarn Install ------ && yarn install && echo ------ Webpack Compile ------ && webpack"
    },
    "dependencies": {
        "@types/node": "^16.10.2",
        "ts-loader": "^9.2.6",
        "typescript": "^4.4.3",
        "webpack": "^5.56.1",
        "webpack-cli": "^4.8.0"
    }
}
